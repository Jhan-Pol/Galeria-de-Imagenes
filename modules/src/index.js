import {finish, reconozing, cards} from "./selectionImgs";
import buttonExit from "./buttonExit";
import {imgs, reconozingCarrusel} from "./carrusel";
finish(cards, reconozing);
if(finish) {
    finish(imgs, reconozingCarrusel);
}